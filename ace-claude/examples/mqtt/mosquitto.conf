# Minimal Mosquitto 2.x config for local testing.
# Without this the broker listens only on localhost INSIDE the container and
# nothing on the host (or in ACE) can reach it.
listener 1883 0.0.0.0
allow_anonymous true
