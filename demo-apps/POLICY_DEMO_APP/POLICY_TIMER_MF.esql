CREATE COMPUTE MODULE POLICY_TIMER_MF_Compute

	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		SET OutputRoot.BLOB.BLOB = CAST('tick ' || CAST(CURRENT_TIMESTAMP AS CHARACTER) AS BLOB CCSID 1208);
		RETURN TRUE;
	END;

END MODULE;
